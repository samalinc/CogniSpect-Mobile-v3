<?xml version="1.0" encoding="utf-8" ?>

<local:AuthenticationBasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CSMobile.Presentation.Views.Pages"
             x:Class="CSMobile.Presentation.Views.Pages.AuthenticationPage"
             NavigationPage.HasNavigationBar="False">

    <StackLayout>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <StackLayout HeightRequest="80" VerticalOptions="Center" Grid.Row="1">
                <ActivityIndicator IsRunning="{Binding IsThinking}"
                                   IsVisible="{Binding IsThinking}"
                                   HeightRequest="80"
                                   VerticalOptions="CenterAndExpand"
                                   HorizontalOptions="CenterAndExpand">
                </ActivityIndicator>
            </StackLayout>
            <Entry Grid.Row="1" Text="{Binding StudNumber, Mode=TwoWay}"/>
            <Button HorizontalOptions="Start" Grid.Row="2" Text="Вход" Command="{Binding AuthenticateCommand}"/>
        </Grid>
    </StackLayout>

</local:AuthenticationBasePage>
